{
  "name": "lykos-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "alkabot"
  ],
  "scripts": {
    "bot:build": "npm --workspace @alkacode/lykos run build",
    "bot:dev": "npm --workspace @alkacode/lykos run dev",
    "bot:test": "npm --workspace @alkacode/lykos run test",
    "bot:typecheck": "npm --workspace @alkacode/lykos run typecheck"
  },
  "engines": {
    "node": ">=24.17.0"
  }
}
